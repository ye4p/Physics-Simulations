#include <physics.h>
#include <iostream>
#include <vector>

void start(int num_of_particles)
{
    std::vector<Molecule> particles;
    for (int i = 0; i < num_of_particles; i++)
    {
        // particles.push_back()
    }
}

int main()
{

    return 0;
}